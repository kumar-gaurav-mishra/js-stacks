'use strict';
const chai = require('chai');
chai.should();
let Stack = require('./index');
describe('Stack', () => {
  let stack;
  beforeEach(() => {
    stack = new Stack();
  });
  describe('Instantiation', () => {});
});
